{
  "name": "system-healthcheck-app",
  "version": "1.0.1",
  "description": "Healthcheck app for monitoring services, databases and APIs.",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=play; node index.js",
    "watch": "export NODE_ENV=play; npm install; node node_modules/nodemon/bin/nodemon.js --watch src --watch config index.js",
    "test": "mocha 'src/**/*.tests.js' --reporter spec"
  },
  "author": "Chris Adams",
  "license": "ISC",
  "dependencies": {
    "config": "^1.30.0",
    "express": "^4.16.3",
    "system-healthcheck": "^1.0.0",
    "winston": "^2.4.1",
    "winston-daily-rotate-file": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.17.2",
    "onchange": "^3.3.0"
  }
}
